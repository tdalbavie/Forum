import React, { createContext, useContext, useState, useEffect } from "react";

const CartContext = createContext();

export const useCart = () => useContext(CartContext);

export const CartProvider = ({ children }) => {
  const [cartItems, setCartItems] = useState([]);

  const addToCart = (product, quantity) => {
    // Implement your logic for adding items to the cart
  };

  const updateCartItem = (itemId, change) => {
    // Implement your logic for updating item quantity in the cart
  };

  useEffect(() => {
    // Implement any side effects related to the cart, such as storing data in localStorage
  }, [cartItems]);

  return (
    <CartContext.Provider value={{ cartItems, addToCart, updateCartItem }}>
      {children}
    </CartContext.Provider>
  );
};
